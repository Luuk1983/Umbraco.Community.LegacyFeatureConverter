@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.RichTextBlockItem>

@*
    THIS VIEW NEEDS TO BE UPDATED

    Auto-generated during macro to block conversion for macro: Insert Form with Theme

    REQUIRED CHANGES:
    1. Replace Model.MacroParameters["x"] with Model.Content.Value<T>("x").
    2. Update any @using statements if necessary.
    3. Test rendering in the backoffice and on the frontend.
    4. Remove this header once migration is complete.

    Available block properties:
    - ExcludeScripts (Umbraco.TrueFalse)
    - FormGuid (UmbracoForms.FormPicker)
    - FormTheme (UmbracoForms.ThemePicker)
    - RedirectToPageId (Umbraco.ContentPicker)

    Migration examples:
      Old: Model.MacroParameters["title"]
      New: Model.Content.Value<string>("title")

    ============================================
    Original macro view code (for reference):
    ============================================
<!-- Original macro view not found -->
    ============================================
*@

<div class="block-migration-warning" style="padding: 20px; background: #fff3cd; border: 2px solid #ffc107; border-radius: 4px; margin: 10px 0;">
  <h4 style="margin-top: 0; color: #856404;">Block requires configuration</h4>
  <p style="margin-bottom: 0; color: #856404;">This block was auto-converted from the macro <strong>Insert Form with Theme</strong>. The view template must be updated by a developer before it will render correctly. See <code>/Views/Partials/richtext/Components/Insert Form with Theme.cshtml</code> for migration instructions.</p>
</div>
